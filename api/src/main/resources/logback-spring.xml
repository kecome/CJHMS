<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true">
    <springProperty scope="context" name="profiles" source="spring.profiles"/>
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>log/cjhms.txt</File>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>log/cjhms-%d{yyyy-MM-dd}.%i.txt</FileNamePattern>
            <!-- each file should be at most 300MB, keep 60 days worth of history, but at most 20GB -->
            <maxFileSize>300MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>20GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread][%X{id}:%X{name}:%X{role}] %-5level %logger{36}-%msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoder 默认配置为PatternLayoutEncoder -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{id}:%X{name}:%X{role}] %-5level %logger{36}-%msg%n</pattern>
        </encoder>
    </appender>

    <appender name="email" class="ch.qos.logback.classic.net.SMTPAppender">
        <smtpHost>smtp.163.com</smtpHost>
        <smtpPort>25</smtpPort>
        <username>13164784101@163.com</username>
        <password>caiji123456</password>
        <SSL>true</SSL>
        <asynchronousSending>true</asynchronousSending>
        <to>741919791@qq.com</to>
        <from>13164784101@163.com</from>
        <subject>【${profiles}】环境error邮件</subject>
        <layout class="ch.qos.logback.classic.html.HTMLLayout">
            <pattern>%date%level%X{id}%X{name}%X{role}%logger{0}%message</pattern>
        </layout>
    </appender>
    <logger name="com.homework" level="INFO" additivity="true">
        <appender-ref ref="email"/>
    </logger>

    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
            <appender-ref ref="file"/>
        </root>
    </springProfile>
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
            <appender-ref ref="file"/>
        </root>
    </springProfile>
    <springProfile name="local">
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
            <appender-ref ref="file"/>
        </root>
    </springProfile>
</configuration>